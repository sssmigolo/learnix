services:
  - type: static
    name: learnix
    env: static
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
